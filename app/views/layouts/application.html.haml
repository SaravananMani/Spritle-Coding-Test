!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title TestProject
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    .main-content
      .main-content-inner
        .container-fluid
          .row-fluid
            .row.well
              .col-xs-10
                .pull-right
                  = link_to "All Contact", root_path, class: 'btn btn-success btn-sm'
                  = link_to "New Contact", contact_new_path, class: 'btn btn-success btn-sm'
              .clearfix
              .space-4
              - flash.each do |key, value|
                - key = 'info' if ['notice','alert'].include?(key)
                .alert.fade.in.center{class: "alert-#{key}"}
                  %button.close{"data-dismiss" => "alert"} x
                  = raw(value)
              = yield
            .clearfix
